import React from 'react'
import { StyleSheet } from 'react-native'

const containerCep = StyleSheet.create({
  box: {
    backgroundColor: '#eee',
    width: '100%',
    height: 300,
    borderRadius: 7,
    justifyContent: 'center',
    alignItems: 'center',
    borderWidth: 2,
    borderColor: '#000'
  }
})

export default containerCep
